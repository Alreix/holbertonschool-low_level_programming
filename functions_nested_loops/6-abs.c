#include "main.h"

/**
 * _abs - Compute the absolute value of integer.
 * @n: The character to check.
 *
 * Description: Compute the absolute value of integer.
 * Return: n
 */
int _abs(int n)
{

	if (n < 0)
	{
		n = -n;
	}
	return (n);
}
